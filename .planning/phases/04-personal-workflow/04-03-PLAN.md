---
phase: 04-personal-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
files_modified:
  - src/server/digests/build-daily-digest.ts
  - src/app/api/digests/daily/route.ts
autonomous: true
requirements:
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - The app can generate a daily digest from current feed items
    - A route exists for reading the latest digest payload
  artifacts:
    - src/server/digests/build-daily-digest.ts
    - src/app/api/digests/daily/route.ts
  key_links:
    - Digest generation reuses feed and explanation services
    - Digest API exposes a compact catch-up payload
---

<objective>
Implement daily digest generation and API.

Purpose: Turn the feed into a quick catch-up product, not just a browse surface.
Output: Digest generation service and latest-digest API route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-explanation-layer/03-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement daily digest generation</name>
  <files>src/server/digests/build-daily-digest.ts</files>
  <action>Create a service that selects a short set of important current items and packages them into a digest structure with intro and short summaries.</action>
  <verify>Check that digest generation returns a bounded list of digest entries.</verify>
  <done>The app has a compact catch-up artifact built from current feed data.</done>
</task>
<task type="auto">
  <name>Task 2: Implement the daily digest API route</name>
  <files>src/app/api/digests/daily/route.ts</files>
  <action>Create the route that returns the latest digest payload in the contract shape expected by the roadmap and spec.</action>
  <verify>Check that the route module exists and delegates to the digest service.</verify>
  <done>The frontend has a stable route for digest consumption.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/digests/build-daily-digest.ts`
- [ ] `test -f src/app/api/digests/daily/route.ts`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can retrieve a compact daily digest of important updates
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-workflow/04-03-SUMMARY.md`
</output>
