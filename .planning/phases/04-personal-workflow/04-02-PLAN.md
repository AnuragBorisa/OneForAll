---
phase: 04-personal-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/server/saved/saved-store.ts
  - src/app/api/items/[itemId]/save/route.ts
  - src/app/api/saved/route.ts
  - src/app/saved/page.tsx
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - Users can save a feed item by ID
    - Saved items can be listed in a dedicated view
  artifacts:
    - src/server/saved/saved-store.ts
    - src/app/api/items/[itemId]/save/route.ts
    - src/app/api/saved/route.ts
    - src/app/saved/page.tsx
  key_links:
    - Save routes use a shared saved-item store
    - Saved page renders the same item/explanation primitives as the main product
---

<objective>
Build save-item and saved-items flows.

Purpose: Give users a way to hold onto useful items and revisit them later.
Output: Save route, saved-items route, saved-items store, and saved-items page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-explanation-layer/03-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement saved-item state and routes</name>
  <files>src/server/saved/saved-store.ts, src/app/api/items/[itemId]/save/route.ts, src/app/api/saved/route.ts</files>
  <action>Create a simple shared saved-item store for the MVP and route handlers for saving an item and listing saved items.</action>
  <verify>Check that route files exist and use the shared saved-item store.</verify>
  <done>Saved item state can be mutated and read through stable API boundaries.</done>
</task>
<task type="auto">
  <name>Task 2: Build the saved-items page</name>
  <files>src/app/saved/page.tsx</files>
  <action>Create a saved-items page that lists saved entries and links back to their detail views or sources.</action>
  <verify>Check that the saved page renders saved items using existing item primitives.</verify>
  <done>Users can revisit saved items in one place.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/app/api/items/[itemId]/save/route.ts`
- [ ] `test -f src/app/api/saved/route.ts`
- [ ] `test -f src/app/saved/page.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can save items and review them later
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-workflow/04-02-SUMMARY.md`
</output>
