---
phase: 04-personal-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/feed-filters.ts
  - src/server/ranking/get-feed.ts
  - src/app/(feed)/page.tsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - Feed results can be filtered by source and topic
    - The feed page exposes usable controls for filtering
  artifacts:
    - src/lib/feed-filters.ts
    - src/server/ranking/get-feed.ts
    - src/app/(feed)/page.tsx
  key_links:
    - Filter logic is shared between route parsing and feed queries
    - Feed page uses filter-aware data loading
---

<objective>
Add feed filtering controls and query support.

Purpose: Let users narrow the feed to the source and topic slices they care about most.
Output: Shared filter helpers, filter-aware feed queries, and feed page controls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-feed/02-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement shared filter parsing and feed query support</name>
  <files>src/lib/feed-filters.ts, src/server/ranking/get-feed.ts</files>
  <action>Create helpers for source and topic filters, then extend the feed service so it can apply those filters cleanly to mixed-source feed results.</action>
  <verify>Check that the feed service accepts source and topic filters and returns a narrowed result set.</verify>
  <done>Filter logic exists outside the page component and can be reused by routes and pages.</done>
</task>
<task type="auto">
  <name>Task 2: Add filter controls to the feed page</name>
  <files>src/app/(feed)/page.tsx</files>
  <action>Update the feed page to read query parameters, display current filters, and provide links or controls for switching source and topic views.</action>
  <verify>Check that the page file exposes filter-aware rendering paths.</verify>
  <done>The feed page supports basic daily-use filtering.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/lib/feed-filters.ts`
- [ ] `test -f src/app/(feed)/page.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can filter feed results by source and topic
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-workflow/04-01-SUMMARY.md`
</output>
