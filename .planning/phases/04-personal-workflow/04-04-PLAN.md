---
phase: 04-personal-workflow
plan: 04
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src/server/digests/render-whatsapp-digest.ts
  - src/app/(feed)/digest/page.tsx
autonomous: true
requirements:
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - Users can view the digest in a dedicated page
    - The digest can be rendered into a WhatsApp-friendly text format
  artifacts:
    - src/server/digests/render-whatsapp-digest.ts
    - src/app/(feed)/digest/page.tsx
  key_links:
    - Digest UI consumes the digest generation service or route
    - WhatsApp rendering uses the same digest content as the page
---

<objective>
Build the digest UI and WhatsApp-ready rendering.

Purpose: Complete the daily catch-up loop and prepare the product for future outbound digest delivery.
Output: Digest page and WhatsApp-friendly digest rendering helper.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-personal-workflow/04-03-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement WhatsApp-ready digest rendering</name>
  <files>src/server/digests/render-whatsapp-digest.ts</files>
  <action>Create a helper that turns the digest payload into concise WhatsApp-ready text with title, short summaries, and links.</action>
  <verify>Check that the helper returns a compact text payload built from digest data.</verify>
  <done>The digest can be reused later for outbound chat delivery.</done>
</task>
<task type="auto">
  <name>Task 2: Build the digest page</name>
  <files>src/app/(feed)/digest/page.tsx</files>
  <action>Create a dedicated digest page that renders the catch-up summary and each digest entry clearly for quick scanning.</action>
  <verify>Check that the page file exists and renders digest entries rather than placeholder content.</verify>
  <done>Users can use the app itself as a daily digest surface.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/digests/render-whatsapp-digest.ts`
- [ ] `test -f src/app/(feed)/digest/page.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The product exposes both an in-app digest view and a WhatsApp-ready digest format
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-workflow/04-04-SUMMARY.md`
</output>
