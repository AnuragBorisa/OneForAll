---
phase: 03-explanation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/pipeline/generate-explanation.ts
  - src/server/pipeline/save-explanation.ts
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
  - EXPL-03
  - EXPL-04
user_setup: []
must_haves:
  truths:
    - The codebase can generate a structured explanation block for any feed item
    - Generated explanations include practical sections beyond a headline summary
  artifacts:
    - src/server/pipeline/generate-explanation.ts
    - src/server/pipeline/save-explanation.ts
  key_links:
    - Explanation generation uses the existing feed model
    - Generated explanation output aligns with the explanation schema
---

<objective>
Implement structured explanation generation for feed items.

Purpose: Add the product's main differentiator by turning feed items into practical explanations with examples and next steps.
Output: Explanation generation and persistence helpers that produce structured explanation payloads.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-feed/02-03-SUMMARY.md
@specs/002-ai-feed-mvp/contracts/feed-api.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement structured explanation generation</name>
  <files>src/server/pipeline/generate-explanation.ts</files>
  <action>Create a service that turns a feed item into a structured explanation payload with simple explanation, why it matters, example, use cases, who should care, next step, and credibility notes. The first version can be deterministic and heuristic-driven.</action>
  <verify>Check that the generator returns all required explanation fields for a sample feed item.</verify>
  <done>The app can generate practical explanation data without relying on the UI to invent it.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explanation persistence helpers</name>
  <files>src/server/pipeline/save-explanation.ts</files>
  <action>Create a helper that packages generated explanation payloads into the app's explanation block shape and versioning pattern for downstream consumption.</action>
  <verify>Check that persistence helpers return explanation blocks aligned with the shared domain shape.</verify>
  <done>Later routes and pages can consume one stable explanation block format.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/pipeline/generate-explanation.ts`
- [ ] `test -f src/server/pipeline/save-explanation.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Structured practical explanations can be generated for feed items
</success_criteria>

<output>
After completion, create `.planning/phases/03-explanation-layer/03-01-SUMMARY.md`
</output>
