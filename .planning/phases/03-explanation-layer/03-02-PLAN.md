---
phase: 03-explanation-layer
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/server/pipeline/credibility-notes.ts
  - src/server/pipeline/publishable-item.ts
autonomous: true
requirements:
  - EXPL-05
user_setup: []
must_haves:
  truths:
    - Credibility notes can be derived from the feed item and explanation content
    - The app can decide whether an explanation is complete enough to publish
  artifacts:
    - src/server/pipeline/credibility-notes.ts
    - src/server/pipeline/publishable-item.ts
  key_links:
    - Publishability checks depend on the structured explanation payload
    - Credibility notes augment, not replace, the core explanation block
---

<objective>
Add credibility notes and publishability rules for explanation output.

Purpose: Ensure the product does not present weak or incomplete explanations as trustworthy finished output.
Output: Credibility note heuristics and publishability checks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-explanation-layer/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement credibility note heuristics</name>
  <files>src/server/pipeline/credibility-notes.ts</files>
  <action>Create a small heuristic module that adds credibility notes or caveats based on source type, content type, and textual signals of uncertainty or promotion.</action>
  <verify>Check that the module returns caveats for at least one weak-signal input and stays quiet for stronger official-style input.</verify>
  <done>The explanation layer can represent uncertainty explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement publishability checks</name>
  <files>src/server/pipeline/publishable-item.ts</files>
  <action>Create a helper that rejects incomplete explanation payloads and exposes a single publishability decision for routes and pages.</action>
  <verify>Check that incomplete payloads are rejected and complete payloads pass.</verify>
  <done>The app has a consistent gate before explanation output is shown as complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/pipeline/credibility-notes.ts`
- [ ] `test -f src/server/pipeline/publishable-item.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Explanation output includes explicit caveats and a usable publishability gate
</success_criteria>

<output>
After completion, create `.planning/phases/03-explanation-layer/03-02-SUMMARY.md`
</output>
