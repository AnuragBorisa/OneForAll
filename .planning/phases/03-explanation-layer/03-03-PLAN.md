---
phase: 03-explanation-layer
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/api/items/[itemId]/route.ts
  - src/app/(feed)/items/[itemId]/page.tsx
  - src/components/explanation-block.tsx
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
  - EXPL-03
  - EXPL-04
  - EXPL-05
user_setup: []
must_haves:
  truths:
    - The app exposes an item detail API returning the item and explanation block
    - Users can open a detail page and read structured practical explanations
  artifacts:
    - src/app/api/items/[itemId]/route.ts
    - src/app/(feed)/items/[itemId]/page.tsx
    - src/components/explanation-block.tsx
  key_links:
    - The detail route uses shared explanation generation and publishability helpers
    - The detail page preserves source attribution and source links
---

<objective>
Build the item detail API and UI for structured explanations.

Purpose: Expose explanation output through a concrete user-facing detail view rather than leaving it as backend-only logic.
Output: Item detail route, explanation UI component, and feed item detail page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/contracts/feed-api.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the item detail API route</name>
  <files>src/app/api/items/[itemId]/route.ts</files>
  <action>Create the item detail route that returns the selected feed item plus its structured explanation block in a contract-aligned shape.</action>
  <verify>Check that the route file exists and can return a 404 for unknown items and a detail payload for known items.</verify>
  <done>The frontend has a stable API boundary for explanation detail views.</done>
</task>

<task type="auto">
  <name>Task 2: Build the explanation component and detail page</name>
  <files>src/components/explanation-block.tsx, src/app/(feed)/items/[itemId]/page.tsx</files>
  <action>Create a reusable explanation block component and the item detail page that renders all explanation sections, preserves attribution, and links back to the source.</action>
  <verify>Check that the page and component files exist and render structured explanation content rather than raw text blobs.</verify>
  <done>Users can open any feed item and understand why it matters and how to use it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/app/api/items/[itemId]/route.ts`
- [ ] `test -f src/app/(feed)/items/[itemId]/page.tsx`
- [ ] `test -f src/components/explanation-block.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The product exposes practical explanation detail views for feed items
</success_criteria>

<output>
After completion, create `.planning/phases/03-explanation-layer/03-03-SUMMARY.md`
</output>
