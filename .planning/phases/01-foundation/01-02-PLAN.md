---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle.config.ts
  - src/types/feed.ts
  - src/server/db/client.ts
  - src/server/db/schema/feed.ts
  - scripts/ingest/migrate.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Canonical source, feed, explanation, digest, and saved-item models exist in code
    - Database access and migration entry points are defined for later implementation
  artifacts:
    - src/types/feed.ts
    - src/server/db/schema/feed.ts
    - src/server/db/client.ts
  key_links:
    - Domain types align with the database schema
    - The schema and client can be reused by source ingestion and feed APIs
---

<objective>
Define the database schema and shared domain types for the AI Feed MVP.

Purpose: Lock the canonical data model so later plans can implement ingestion, feed queries, explanations, and digests against the same shape.
Output: Shared feed types, Drizzle configuration, database client, schema definitions, and migration entry points.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/data-model.md
@specs/002-ai-feed-mvp/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical feed domain types</name>
  <files>src/types/feed.ts</files>
  <action>Translate the spec data model into shared TypeScript domain types for source items, feed items, story clusters, explanation blocks, digests, and saved items. Include controlled enums or literal unions for source families and processing states.</action>
  <verify>Review the file to confirm every key entity from the spec has a corresponding type.</verify>
  <done>The domain model can be imported consistently across the app and pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Create database client and schema definitions</name>
  <files>drizzle.config.ts, src/server/db/client.ts, src/server/db/schema/feed.ts, scripts/ingest/migrate.ts</files>
  <action>Define the initial Drizzle configuration, database client, and schema tables that mirror the canonical types. Add a migration entry point suitable for later automation.</action>
  <verify>Check that the schema covers raw source items, normalized feed items, explanations, digests, and saved items.</verify>
  <done>The persistence layer is defined clearly enough for downstream plans to use without redesign.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/types/feed.ts`
- [ ] `test -f src/server/db/schema/feed.ts`
- [ ] `test -f src/server/db/client.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The canonical model exists in both shared types and database schema form
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
