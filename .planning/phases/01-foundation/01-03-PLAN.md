---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/server/ingestion/adapters/types.ts
  - src/server/pipeline/normalize-item.ts
  - src/server/pipeline/explanation-schema.ts
  - src/server/ingestion/jobs/run-pipeline.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - A processing pipeline boundary exists for fetch, normalize, validate, and publishability stages
    - Source adapters share one normalization contract
  artifacts:
    - src/server/ingestion/adapters/types.ts
    - src/server/pipeline/normalize-item.ts
    - src/server/pipeline/explanation-schema.ts
    - src/server/ingestion/jobs/run-pipeline.ts
  key_links:
    - Pipeline modules import the shared domain types and schema concepts from prior plans
    - The orchestration entry point can call the normalization and validation boundaries
---

<objective>
Create the pipeline orchestration and normalization boundaries for later ingestion work.

Purpose: Define how raw source payloads become canonical feed items and how explanation blocks are validated before publication.
Output: Source adapter interfaces, normalization service, explanation validation boundary, and pipeline orchestration entry points.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/plan.md
@specs/002-ai-feed-mvp/data-model.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define source adapter and normalization interfaces</name>
  <files>src/server/ingestion/adapters/types.ts, src/server/pipeline/normalize-item.ts</files>
  <action>Create the shared interfaces and service boundary that every source adapter will use to produce canonical feed items. Keep the contract strict enough to support later duplicate detection and explanation generation.</action>
  <verify>Review the interfaces to confirm all source families can map into the same normalized item shape.</verify>
  <done>Future source adapters can be implemented without redefining core pipeline contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Define explanation validation and pipeline orchestration entry points</name>
  <files>src/server/pipeline/explanation-schema.ts, src/server/ingestion/jobs/run-pipeline.ts</files>
  <action>Create the explanation schema validator and a high-level orchestration module covering fetch, normalize, cluster, explain, and digest stages. The orchestration can be skeletal, but stage boundaries must be explicit and reusable.</action>
  <verify>Check that the orchestration references the intended stages and that explanation validation enforces the required practical summary fields.</verify>
  <done>The repository has a clear processing backbone ready for source-specific work in Phase 2.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/ingestion/adapters/types.ts`
- [ ] `test -f src/server/pipeline/normalize-item.ts`
- [ ] `test -f src/server/ingestion/jobs/run-pipeline.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The pipeline and validation boundaries are explicit enough for feed implementation to proceed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
