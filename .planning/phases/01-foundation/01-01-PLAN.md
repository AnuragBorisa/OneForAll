---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/env.ts
  - .env.example
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - The repository has a runnable Next.js TypeScript application structure
    - Environment configuration is defined and documented for local development
  artifacts:
    - package.json
    - src/app/layout.tsx
    - src/lib/env.ts
    - .env.example
  key_links:
    - The root app bootstraps from the configured Next.js application files
    - Environment helpers are reusable by later server-side modules
---

<objective>
Initialize the application structure and developer tooling for the AI Feed MVP.

Purpose: Establish the runnable app shell and environment boundary that every later phase depends on.
Output: App bootstrap files, root app routes, and environment configuration helpers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Next.js TypeScript application shell</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx</files>
  <action>Create the root package and application files for a minimal but runnable Next.js TypeScript app. Keep the initial UI intentionally simple and oriented around the AI Feed concept instead of placeholder boilerplate.</action>
  <verify>Test that the root config files exist and the app entry files are present.</verify>
  <done>The repository contains a coherent app shell that later plans can build on directly.</done>
</task>

<task type="auto">
  <name>Task 2: Define environment loading and examples</name>
  <files>.env.example, src/lib/env.ts</files>
  <action>Create a single environment helper and an example environment file covering database, source adapters, and AI provider configuration. Keep names stable and easy to extend.</action>
  <verify>Confirm the environment helper and example file cover all known Phase 1 and Phase 2 external dependencies.</verify>
  <done>Future plans can import one shared environment module instead of scattered variables.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f package.json`
- [ ] `test -f src/app/layout.tsx`
- [ ] `test -f src/lib/env.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app shell is ready for schema and pipeline work in later plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
