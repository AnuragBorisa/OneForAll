---
phase: 05-quality-and-operations
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - scripts/ingest/reprocess-items.ts
  - scripts/ingest/run-digest.ts
autonomous: true
requirements:
  - OPS-03
user_setup: []
must_haves:
  truths:
    - Operators can rerun explanation/item reprocessing from a script
    - Operators can regenerate the digest from a script
  artifacts:
    - scripts/ingest/reprocess-items.ts
    - scripts/ingest/run-digest.ts
  key_links:
    - Operator scripts reuse existing app services
    - Scripts produce inspectable output for manual operations
---

<objective>
Add operator reprocessing and digest rerun workflows.

Purpose: Make the app operable when explanation logic changes or digest outputs need to be regenerated.
Output: Operator scripts for reprocessing items and rerunning digest generation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/detail/get-item-detail.ts
@src/server/digests/build-daily-digest.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add item reprocessing script</name>
  <files>scripts/ingest/reprocess-items.ts</files>
  <action>Create a script that reassembles explanation detail for one or more item IDs and prints the result for operator review.</action>
  <verify>Check that the script delegates to shared detail/explanation services.</verify>
  <done>Operators can rerun item processing without entering the app UI.</done>
</task>
<task type="auto">
  <name>Task 2: Add digest rerun script</name>
  <files>scripts/ingest/run-digest.ts</files>
  <action>Create a script that regenerates the current digest payload and prints both the structured digest and the WhatsApp-ready render.</action>
  <verify>Check that the script reuses digest services and rendering helpers.</verify>
  <done>Operators can rerun digest generation from the command line path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f scripts/ingest/reprocess-items.ts`
- [ ] `test -f scripts/ingest/run-digest.ts`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators have scripts for reprocessing and digest regeneration
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-and-operations/05-03-SUMMARY.md`
</output>
