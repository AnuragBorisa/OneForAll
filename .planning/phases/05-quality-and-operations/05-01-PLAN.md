---
phase: 05-quality-and-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/source-items.json
  - tests/fixtures/explanation-eval.json
  - scripts/ingest/seed-fixtures.ts
autonomous: true
requirements:
  - OPS-03
user_setup: []
must_haves:
  truths:
    - Repeatable fixture sets exist for source normalization and explanation evaluation
    - Fixture seeding has a dedicated script entry point
  artifacts:
    - tests/fixtures/source-items.json
    - tests/fixtures/explanation-eval.json
    - scripts/ingest/seed-fixtures.ts
  key_links:
    - Fixture files map to the same domain shapes used by the app
    - Seeding script references the fixture files directly
---

<objective>
Add fixtures and evaluation coverage.

Purpose: Make the product easier to inspect and evolve without depending on live services for every validation pass.
Output: Source and explanation fixture files plus a fixture seeding script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/quickstart.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create repeatable fixture files</name>
  <files>tests/fixtures/source-items.json, tests/fixtures/explanation-eval.json</files>
  <action>Create concrete fixture sets representing mixed-source items and expected explanation evaluation examples for manual and future automated checks.</action>
  <verify>Check that fixture files exist and contain structured records rather than placeholders.</verify>
  <done>Fixtures can drive normalization and explanation review consistently.</done>
</task>
<task type="auto">
  <name>Task 2: Add fixture seeding entry point</name>
  <files>scripts/ingest/seed-fixtures.ts</files>
  <action>Create a script that reads the fixture files and reports or prepares them for local validation workflows.</action>
  <verify>Check that the script references the fixture files and produces a coherent output shape.</verify>
  <done>The repo has an explicit entry point for fixture-based validation work.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f tests/fixtures/source-items.json`
- [ ] `test -f tests/fixtures/explanation-eval.json`
- [ ] `test -f scripts/ingest/seed-fixtures.ts`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Fixture-backed validation can be repeated locally
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-and-operations/05-01-SUMMARY.md`
</output>
