---
phase: 02-unified-feed
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/api/feed/route.ts
  - src/app/(feed)/page.tsx
  - src/components/feed-card.tsx
  - src/components/source-badge.tsx
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-04
user_setup: []
must_haves:
  truths:
    - The app exposes a feed API returning mixed-source feed records
    - The main feed page shows mixed-source cards with source attribution and links
  artifacts:
    - src/app/api/feed/route.ts
    - src/app/(feed)/page.tsx
    - src/components/feed-card.tsx
    - src/components/source-badge.tsx
  key_links:
    - The feed page reads from the feed API or shared feed service
    - Feed cards preserve source attribution and source URLs
---

<objective>
Build the feed API and primary mixed-source feed page.

Purpose: Turn the Phase 2 source and ranking work into a visible user-facing unified feed.
Output: Feed API route, main feed page, feed cards, and source attribution UI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/002-ai-feed-mvp/contracts/feed-api.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the feed API route</name>
  <files>src/app/api/feed/route.ts</files>
  <action>Create the feed route that returns mixed-source feed records aligned with the feature contract. Keep the first version simple and typed.</action>
  <verify>Check that the route module exists and returns a coherent JSON feed shape.</verify>
  <done>The feed UI has a stable API boundary for loading mixed-source items.</done>
</task>

<task type="auto">
  <name>Task 2: Build the main feed page and reusable card components</name>
  <files>src/app/(feed)/page.tsx, src/components/feed-card.tsx, src/components/source-badge.tsx</files>
  <action>Create the main feed page and reusable UI pieces that render title, source, author, publish time, and source link clearly. Keep the layout consistent with the product direction established in Phase 1.</action>
  <verify>Check that the page and component files exist and preserve source attribution in the UI shape.</verify>
  <done>The product has a visible unified feed instead of only pipeline internals.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/app/api/feed/route.ts`
- [ ] `test -f src/app/(feed)/page.tsx`
- [ ] `test -f src/components/feed-card.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A user can see mixed-source feed items with source attribution and source links
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-feed/02-03-SUMMARY.md`
</output>
