---
phase: 02-unified-feed
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/server/ranking/cluster-stories.ts
  - src/server/ranking/get-feed.ts
  - src/server/db/repositories/feed-repository.ts
autonomous: true
requirements:
  - FEED-01
  - FEED-03
user_setup: []
must_haves:
  truths:
    - Feed items can be grouped or reduced heuristically before rendering
    - A server-side feed query service can produce mixed-source feed output
  artifacts:
    - src/server/ranking/cluster-stories.ts
    - src/server/ranking/get-feed.ts
    - src/server/db/repositories/feed-repository.ts
  key_links:
    - Ranking uses normalized feed items from the Phase 1 foundation and Phase 2 adapters
    - Feed queries can return clustered output to the API layer
---

<objective>
Build duplicate reduction and feed ranking services for the unified AI feed.

Purpose: Prevent the mixed-source feed from becoming noisy and give the API a clean server-side read model.
Output: Story clustering heuristics, feed query service, and repository reads for feed output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feed repository reads for mixed-source output</name>
  <files>src/server/db/repositories/feed-repository.ts</files>
  <action>Create repository functions for listing feed items, finding related items, and returning source attribution data in a UI-ready shape.</action>
  <verify>Check that repository exports cover feed listing and item lookup use cases.</verify>
  <done>The feed API has one clear server-side read layer for mixed-source content.</done>
</task>

<task type="auto">
  <name>Task 2: Add story clustering and feed ranking services</name>
  <files>src/server/ranking/cluster-stories.ts, src/server/ranking/get-feed.ts</files>
  <action>Create heuristic clustering and feed assembly logic that reduce duplicate stories and return a readable feed view. Keep heuristics simple and explicit for the MVP.</action>
  <verify>Check that the feed service composes repository data with clustering output.</verify>
  <done>The API layer can request a single mixed-source feed instead of stitching logic together itself.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/ranking/cluster-stories.ts`
- [ ] `test -f src/server/ranking/get-feed.ts`
- [ ] `test -f src/server/db/repositories/feed-repository.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The feed layer reduces obvious duplicate stories before UI rendering
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-feed/02-02-SUMMARY.md`
</output>
