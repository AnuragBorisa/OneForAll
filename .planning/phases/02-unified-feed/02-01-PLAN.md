---
phase: 02-unified-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/ingestion/adapters/rss.ts
  - src/server/ingestion/adapters/youtube.ts
  - src/server/ingestion/adapters/reddit.ts
  - src/server/ingestion/adapters/x.ts
  - scripts/ingest/run-fixtures.ts
autonomous: true
requirements:
  - FEED-01
  - FEED-02
user_setup: []
must_haves:
  truths:
    - Source adapter files exist for all four source families
    - Each adapter returns source items matching the shared contract
    - Fixture-backed ingestion can run without live credentials
  artifacts:
    - src/server/ingestion/adapters/rss.ts
    - src/server/ingestion/adapters/youtube.ts
    - src/server/ingestion/adapters/reddit.ts
    - src/server/ingestion/adapters/x.ts
    - scripts/ingest/run-fixtures.ts
  key_links:
    - All adapters implement the shared SourceAdapter interface
    - Fixture ingestion can exercise the adapters and pipeline without network access
---

<objective>
Implement source adapter foundations for RSS/blogs, YouTube, Reddit, and X.

Purpose: Give the unified feed phase real inputs from each source family while keeping local development and evaluation possible through fixtures.
Output: Four source adapter modules and a fixture-backed ingestion entry point.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@specs/002-ai-feed-mvp/tasks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement source adapter foundations for all source families</name>
  <files>src/server/ingestion/adapters/rss.ts, src/server/ingestion/adapters/youtube.ts, src/server/ingestion/adapters/reddit.ts, src/server/ingestion/adapters/x.ts</files>
  <action>Create concrete adapters for RSS/blogs, YouTube, Reddit, and X that implement the shared SourceAdapter interface. Where live credentials are unavailable, keep behavior explicit and fixture-friendly rather than pretending to perform network fetches.</action>
  <verify>Check that each file exports a source-specific adapter implementing the shared contract.</verify>
  <done>All four source families have a concrete adapter module ready for later live integration.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-backed ingestion entry point</name>
  <files>scripts/ingest/run-fixtures.ts</files>
  <action>Create a local script that feeds sample source records through the adapters and normalization boundary so feed development can progress without relying on external APIs.</action>
  <verify>Check that the fixture script can import the adapters and pipeline modules coherently.</verify>
  <done>The feed phase has a local path for exercising source ingestion without live services.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/server/ingestion/adapters/rss.ts`
- [ ] `test -f src/server/ingestion/adapters/x.ts`
- [ ] `test -f scripts/ingest/run-fixtures.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Source-specific ingestion can proceed without changing the shared adapter contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-feed/02-01-SUMMARY.md`
</output>
